name: docs-build

on:
  push:
    branches:
      - main
  pull_request: ~

jobs:
  preview:
    uses: elastic/docs-builder/.github/workflows/preview-build.yml@main
    with:
      continue-on-error: false
      strict: false
    permissions:
      contents: read
      pull-requests: read
